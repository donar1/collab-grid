# 业务规则确认记录

本文整理当前系统在继续做订单系统、结算系统、佣金脚本前已经确认和仍待确认的问题。已经确认的规则按当前版本执行，待确认项保留给订单界面和结算脚本设计阶段处理。

## 已确认规则

| 事项 | 当前确认结果 |
| --- | --- |
| 同渠道多负责人 | 佣金只给最新审批通过的一条有效记录 |
| 单品合作区商品必填 | `分组=单品合作区` 且商品为空时，不允许审批通过 |
| 现有产品负责人 | 看同一商品、同一渠道、同一合作关系下已经存在的负责人；资源状态为已解绑的不参与佣金分配 |
| 审批后保护 | 审批通过后保护合作渠道、合作关系、商品、分组、申请人、判断、审批结果；状态和奖金允许后续脚本更新 |
| 奖金字段 | 今日奖金、月度奖金、累计奖金使用金额字段；奖金明细使用多行文本 |
| 月度奖金 | 自然月归零，每月 1 日从 0 开始 |
| 重复结算 | 同一天重复执行结算脚本时，月度奖金和累计奖金必须防止重复累加 |
| 结算标记 | 订单侧增加佣金已结算、佣金结算批次等标记 |
| 退款订单 | 退款要冲回历史佣金，不只是排除新增佣金 |
| 状态判断 | 状态脚本排除退款订单，排除已取消订单；撤单规则先保留等待确认 |
| 已解绑 | 由脚本判断更新，不需要单独设置人工权限 |
| 截单口径 | 按财务付款时间作为业务归属日期，具体方案见下文 |
| 订单默认分区 | 新订单默认进入下单区 |
| 产品默认值 | 选择产品后带出应收金额、应付金额和默认收款方 |
| 快照保护 | 快照字段一旦写入不可覆盖，后续只允许通过专门修正流程处理 |
| 已结算保护 | 佣金已结算后，订单的产品、付款方、收款方等关联不允许改动 |

## 业务锁定区

### 同渠道多负责人

当前数据结构允许同一渠道、同一合作关系下存在多条已生效的关联记录，也就是多个负责人同时绑定同一渠道链路。

佣金分配规则已确认：

```text
同一渠道 + 同一合作关系 + 同一商品匹配到多条记录时，只给最新审批通过的一条有效记录。
资源状态 = 已解绑 的记录不参与佣金分配。
```

后续佣金结算脚本需要按审批时间或记录更新时间排序，选择最新通过记录。

### 单品合作区的商品必填

业务规则里写明：

```text
分组 = 单品合作区 时，商品必填
分组 = 渠道负责区 时，商品可以为空
```

已在审批通过动作中增加后端校验：

```text
如果 分组=单品合作区 且 商品为空，则不允许审批通过
```

### 现有产品负责人字段

该字段的业务含义已确认：

```text
同一商品、同一渠道、同一合作关系下已经存在的负责人。
资源状态 = 已解绑 的记录不参与判断。
```

这不是简单 lookup 能完全表达的逻辑，后续应由脚本或专门接口计算。

### 审批后是否封账

资源档案审批后会封账，但业务锁定区审批后目前不封账。

原因是业务锁定区后续还会被状态脚本每天更新：

```text
资源状态：活跃 / 正常 / 沉淀 / 已解绑
今日奖金 / 月度奖金 / 累计奖金
```

已改为字段级保护。审批通过后，以下字段不允许普通编辑继续改：

```text
合作渠道
合作关系
商品
分组
申请人
判断
审批结果
```

以下字段仍允许后续脚本更新：

```text
资源状态
今日奖金
月度奖金
累计奖金
奖金明细
```

## 佣金结算

### 奖金字段类型

当前已修订为：

```text
今日奖金 = currency
月度奖金 = currency
累计奖金 = currency
奖金明细 = multiLineText
```

### 月度奖金的归零规则

需求里写的是：

```text
月度奖金 = 原月度奖金 + 今日奖金
```

月度奖金已确认按自然月归零：

```text
每月 1 日从 0 开始。
```

后续结算脚本需要记录结算月份，避免跨月累加错误。

### 今日奖金覆盖写入

今日奖金规则是覆盖写入：

```text
每次结算重新计算当天所有订单佣金，覆盖旧值
```

已确认同一天重复执行结算脚本时，月度奖金和累计奖金也必须防止重复累加。

订单侧需要增加：

```text
订单.佣金已结算 = true
订单.佣金结算批次 = YYYYMMDD
```

这样脚本重复执行时不会重复累加月度和累计奖金。

### 退款订单

当前规则排除：

```text
事由 = 退款订单
```

已确认退款要冲回历史佣金。后续退款订单需要找到原订单和原佣金分配记录，生成冲回记录或负向佣金。

## 资源状态脚本

### 状态判定基准

截单口径按财务付款时间归属业务日期，而不是单纯按订单创建时间。

建议方案：

```text
1. 订单增加 财务付款时间 字段。
2. 状态脚本和佣金脚本都按 财务付款时间 计算业务日期。
3. 财务付款时间为空的订单不进入完结结算池。
4. 每次脚本运行时读取目标业务日期，例如 2026-06-21。
5. 订单.佣金结算批次 使用业务日期，例如 COMM-20260621。
```

这样可以处理晚录入、跨日付款、补付款等情况。

### 连续窗口算法

渠道负责区使用 30 天窗口，单品合作区使用 7 天窗口。

状态判断规则已确认：

```text
每天至少 1 单。
排除退款订单。
排除已取消订单。
撤单规则先保留等待确认。
```

佣金结算仍需要 `快照毛利 > 0`，但状态活跃判断不直接用佣金结果代替。

### 已解绑状态

`已解绑` 不参与任何匹配和结算。

已确认 `已解绑` 由脚本判断更新，不需要单独设置人工权限。

## 订单系统依赖

订单管理区已经建立。当前字段结构已覆盖下单、待办、完结、跟单、返利、财务、快照、佣金控制和退款追溯。

已建立的关键字段：

| 字段 | 说明 |
| --- | --- |
| 付款方 | 关联资源档案中心，客户侧匹配 |
| 收款方 | 关联资源档案中心，供应商侧匹配 |
| 产品 | 关联产品信息区 |
| 产品ID | lookup 产品信息区.产品ID |
| 分区 | 至少包含完结区 |
| 事由 | 至少包含退款订单 |
| 毛利 | 公式或金额 |
| 实收金额 | 金额 |
| 实付金额 | 金额 |
| 快照毛利 | 结算脚本固化 |
| 快照实收 | 结算脚本固化 |
| 快照实付 | 结算脚本固化 |
| 快照产品名 | 结算脚本固化 |
| 快照付款方名 | 结算脚本固化 |
| 快照收款方名 | 结算脚本固化 |
| 佣金已结算 | 复选框 |
| 佣金结算批次 | 文本或日期 |

当前实现还包含：

```text
内部订单号
订单状态
不能下单原因
财务付款时间
原订单
风险预警
下单复制区
单号预警
跟单复制区
钓鱼预警
```

产品选择后的默认带出规则：

```text
产品信息区.售价 -> 订单管理区.应收金额
产品信息区.成本 -> 订单管理区.应付金额
产品信息区.供应商 -> 订单管理区.收款方
```

订单保护规则：

```text
分区=完结区 且 完结日期已有值时，完结日期不可改。
分区=完结区 且 财务付款时间已有值时，财务付款时间不可改。
快照毛利、快照实收、快照实付、快照产品名、快照付款方名、快照收款方名一旦写入不可覆盖。
佣金结算批次一旦写入不可覆盖。
佣金已结算=true 后，订单关联字段不可修改。
```

这些字段不齐，状态脚本和佣金脚本都不能稳定运行。

## 结算系统依赖

结算系统还需要确认几个边界：

| 问题 | 影响 |
| --- | --- |
| 奖金是否直接进入资金流水 | 影响财务确认流程 |
| 月度奖金是否生成结算单 | 影响结算单结构 |
| 结算后是否封账业务锁定记录 | 影响后续状态更新 |
| 红冲退款是否生成负向流水 | 影响累计奖金 |
| 财务确认后是否允许回滚 | 影响审计和权限 |

建议结算系统不要直接改业务锁定区的历史记录，而是生成独立的结算单和资金流水。

## 权限和审计

### 审批权限

当前 `approver/admin/owner` 可以执行业务锁定审批。

还需要确认是否要细分：

```text
资源审批人
业务锁定审批人
财务审批人
```

如果后续审批场景变多，单一 `approver` 角色可能不够。

### 脚本写入身份

后续状态脚本和佣金脚本会自动写入数据。

需要确认脚本写入时使用什么身份：

| 方案 | 说明 |
| --- | --- |
| 系统用户 | 审计日志更清晰 |
| owner 身份 | 实现简单，但审计不够准确 |
| 执行人身份 | 适合手动触发，不适合定时任务 |

建议新增系统用户或系统操作标识。

### 状态变更日志粒度

状态变更日志已经建表，但还没写脚本。

需要确认什么情况下写日志：

```text
只有状态发生变化才写
还是每天都写一条状态检查记录
```

建议只在状态变化时写日志，避免上千资源每天产生大量无意义日志。

## 当前建议优先级

### 优先确认

1. 同渠道多负责人佣金分配方案
2. 奖金字段是否从多行文本改成金额
3. 月度奖金归零规则
4. 退款订单是否冲回历史佣金
5. 单品合作区商品必填的校验时机

### 后续开发顺序

1. 建立订单管理区字段
2. 建立订单快照字段
3. 写资源状态变更脚本
4. 写佣金结算脚本
5. 建立结算单和资金流水
6. 增加红冲、退款、回滚规则
