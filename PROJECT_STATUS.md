# CollabGrid 项目状态

更新时间：2026-06-21

## 当前已完成

- 工作空间支持重命名、删除。
- 字段支持重命名、删除、锁定。
- 单选字段支持维护选项，每个选项可单独设置颜色。
- 字段宽度可拖拽调整。
- 行高可拖拽调整。
- 行删除已改为明确的「删除」按钮，避免拖拽行高误删。
- 关联字段支持配置目标表和显示列。
- 关联字段支持单选/多选配置：
  - 单选模式：选择新记录会替换旧关联，后端也会强制只保留一条。
  - 多选模式：同一行可关联多条目标记录。
- 关联弹窗支持搜索。
- 新增 `lookup` 关联字段类型：
  - 联动：随源表数据变化实时显示。
  - 锁定：建立关联时保存一次性快照，源数据后续变化不影响已带出的值。
- 表格支持多单元格选择。
- 表格支持复制选区为 TSV。
- 表格支持粘贴 TSV：
  - 有选区时从选区左上角开始粘贴。
  - 点「添加一行 / 粘贴多行」或无选区追加时，会自动创建多行。
- 支持键盘导航：
  - 方向键：上下左右移动。
  - `Tab` / `Shift + Tab`：同行左右移动。
  - `Enter` / `Shift + Enter`：同列上下移动。
- 表格已区分单元格选中态和输入编辑态：
  - 默认单击只选中单元格，不直接编辑。
  - `F2`、双击或直接输入字符进入编辑态。
  - 编辑态中方向键留给输入框移动光标，不跳格。
  - `Esc` 取消编辑并恢复原值。
  - `Enter` / `Tab` 提交编辑并移动到下一格。
  - `Delete` / `Backspace` 可清空当前选区内可写单元格。
- 已完成 P0/P1 工程化改进：
  - 新增 `public/modules/clipboard.js`，拆出剪贴板解析、选区归一化、TSV 单元格清理。
  - 新增 `public/modules/keyboard.js`，拆出键盘移动计算。
  - 新增 `public/modules/fields.js`，拆出单选项和关联字段配置工具。
  - 新增 `public/modules/links.js`，拆出关联弹窗文案与摘要逻辑。
  - 新增 `tests/frontend_logic_test.js`，覆盖剪贴板、键盘、字段和关联模块逻辑。
  - lookup 配置已增加后端校验，避免源字段和关联目标表不匹配。
- 已完成业务系统 P0 底座：
  - 基础快照改为每表首屏分页，单表分页接口支持 20-500 条一页，避免几十万记录一次性加载。
  - 数据库补充记录分页索引、单元格字段索引、关联目标索引。
  - 新增 `autoNumber`、`formula`、`currency`、`button`、`createdTime`、`checkbox` 字段类型。
  - 新增「业务模板」入口，可生成产品、销售订单、采购订单、库存、结算、退款、客户账户、资金流水、预存设备、设备使用记录等表。
  - 新增记录级封账，封账后禁止直接编辑单元格、删除记录、修改关联。
  - 新增按钮执行接口，当前支持封账和解除封账动作。
  - 新增批量事务接口，后续订单、库存、资金脚本可基于它做一次成功或一次失败的跨表写入。
  - 新增 `tests/p0_api_test.js`，覆盖业务模板、自动编号、公式、按钮封账、分页接口。
- 已完成资源档案中心基础表单：
  - 新增「资源档案」入口，只创建一张「资源档案中心」底表。
  - 新增 `attachment` 字段类型，用于身份证明、营业执照、图片或凭证链接。
  - 资源档案中心包含代码、入档日期、企业名称、对接人、电话、地址、身份信息、身份证明、审批领导、审批意见、审批状态、数据可使用、建群对接、业务备注、待办、审批按钮。
  - 审批按钮支持资源审批通过，会更新审批状态、数据可使用、待办、建群对接，并封账当前记录。
  - 单页面新增当前页筛选、字段显示自定义和同表规则视图。
  - 规则视图支持全部档案、待审批、领导审批、已通过可用、待办处理，不再为审批领导或待办生成独立数据表。
  - 新增 `tests/resource_archive_test.js`，覆盖资源档案单表模板和审批通过流程。
- 已完成产品信息区基础表单：
  - 新增「产品信息」入口，创建「产品名称数据源区」和「产品信息区」。
  - 产品名称数据源区维护产品名称、货号、数据状态、备注。
  - 产品信息区包含标题、更新时间、名称、规格、地区、货号、销售规则、下单方式、产品状态、销售状态、详情、售价、税费、税种、供应商、成本、历史供应商、产品分类。
  - 名称从产品名称数据源区选择，货号通过 lookup 从数据源区联动引用。
  - 新增 `textFormula` 合成文字字段，标题自动合并名称、规格、地区、销售规则、产品状态、货号。
  - 新增 `tests/product_info_test.js`，覆盖产品信息模板、名称关联和货号 lookup 配置。
- 已完成团队协作与大数据引用底座：
  - 新增工作区角色：owner、admin、approver、finance、editor、viewer。
  - 邀请协作者时可选择角色，成员面板可由 owner/admin 调整成员角色。
  - viewer 只读，不能新增记录、改单元格、改关联、执行按钮；approver 可执行资源审批；owner/admin 可管理结构和成员。
  - 手动解封限制为 owner/admin，避免审批后普通成员绕过封账。
  - 新增 `/api/tables/:tableId/search` 服务端搜索接口，关联选择器改为按关键词搜索目标表，支持上万产品和上千资源的关联选择。
  - 新增 `tests/team_scale_test.js`，覆盖角色邀请、角色调整、审批权限、只读限制和 130 条数据源尾部搜索。
- 已完成业务锁定区基础表单：
  - 新增「业务锁定」入口，要求先有资源档案中心和产品信息区。
  - 创建「业务锁定区」，并在缺失时创建「员工档案中心」「状态变更日志」。
  - 业务锁定区包含合作渠道、合作关系、商品、分组、判断、申请人、资源状态、审批结果、申请原因、申请日期、审批人、审批时间、粘贴凭证、渠道负责人、现有产品负责人、产品ID、今日奖金、月度奖金、累计奖金、奖金明细、审批通过。
  - 产品信息区新增 `产品ID` 自动编号，业务锁定区的产品ID通过商品关联 lookup 引用。
  - 新增 `multiLineText`、`lastModifiedTime`、`lastModifiedBy` 字段类型。
  - 新增 `approve_business_lock` 按钮动作，审批通过后把判断改为关联、审批结果改为已通过。
  - 业务锁定审批已增加单品合作区商品必填校验，审批后核心绑定字段进入记录级保护。
  - 今日奖金、月度奖金、累计奖金已改为金额字段，奖金明细保留多行文本。
  - 佣金规则已确认：同渠道多负责人只给最新审批通过的有效记录，退款冲回历史佣金，月度奖金按自然月每月 1 日归零，结算批次防止重复累加。
  - 服务端分页接口现在会返回 live lookup 计算值，方便后续结算脚本直接读取产品ID、负责人等联动值。
  - 新增 `tests/business_lock_test.js`，覆盖依赖校验、字段结构、关联、lookup 和审批动作。
- 已完成订单管理区基础表单：
  - 新增「订单管理」入口，要求先有资源档案中心和产品信息区。
  - 创建「订单管理区」，覆盖下单区、待办区、完结区、跟单、返利、财务、快照、佣金控制和退款追溯字段。
  - 新订单默认写入 `分区=下单区`、`事由=订单`、`佣金已结算=false`。
  - 选择产品后自动带出产品售价到应收金额、产品成本到应付金额，并把产品供应商关联为收款方。
  - 订单产品ID通过产品字段 lookup 引用产品信息区产品ID。
  - 完结区内已填写的完结日期和财务付款时间不可再改；快照字段和佣金结算批次一旦写入不可覆盖。
  - 佣金已结算后，订单关联字段不可再改，避免结算后产品/付款方/收款方漂移。
  - 新增 `tests/order_management_test.js`，覆盖依赖校验、默认值、自动带出、公式、lookup、完结保护、快照保护和已结算保护。
- 已完成作业中心第一版：
  - 新增「作业中心」入口，支持选择业务日期、试算模式、手动执行资源状态作业和佣金结算作业。
  - 新增 `job_configs`、`job_runs`、`commission_ledger`、`order_activity_daily` 四张底层表。
  - 资源状态作业会先按财务付款时间重建订单日活动聚合，再按渠道负责区 30 天、单品合作区 7 天窗口更新资源状态。
  - 佣金结算作业会写订单快照、写佣金流水、汇总今日/月度/累计奖金、写奖金明细、标记订单佣金已结算和佣金结算批次。
  - 佣金汇总从 `commission_ledger` 反算，重复执行不会重复累加月度奖金和累计奖金。
  - 退款订单已进入冲回通道：通过原订单查找历史佣金流水，生成负向 `refund_reverse` 流水。
  - 新增 `tests/jobs_test.js`，覆盖 dry-run、正式执行、状态变更、佣金流水、奖金写回、订单标记和重复执行防重。
- 已完成诊断中心与批量订单导入：
  - 新增「诊断中心」入口，按业务日期检查订单、快照、佣金流水、退款原订单、已解绑佣金和失败作业。
  - 新增 `/api/bases/:baseId/diagnostics` 只读诊断接口，为后续智能体排查故障提供结构化问题列表。
  - 新增 `/api/bases/:baseId/bulk/orders` 批量订单导入接口，单次最多 5000 条。
  - 批量导入支持产品、付款方、收款方、原订单关联，并自动带出产品售价、成本和默认供应商。
  - 新增 `tests/diagnostics_bulk_test.js`，覆盖批量导入、默认金额带出、默认收款方带出和诊断问题识别。
- 已优化订单管理区布局：
  - 顶部功能入口改为表名栏右键菜单，避免按钮占用过多横向空间，也避免在任意区域误触菜单。
  - 字段设置改为字段表头右键菜单，包含重命名、选项/关联配置、锁定、宽度提示和删除。
  - 新增「字段管理」统一界面，集中控制字段显示/隐藏。
  - 字段管理内新增字段排序控制，每个字段可上移/下移，自定义顺序保存到字段 `position`。
  - 字段管理内新增单元格格式编辑区，支持对当前选中单元格设置字号、字体颜色、单元格颜色。
  - 字段显示/隐藏保存到本地视图配置，刷新后仍保留；单元格格式保存到 `cells.style_json`，不会一次性改整列或整行。
  - 数据表名称标签支持横向滚动、省略显示和悬停完整名称，当前选中表给更大显示宽度。
  - 订单管理区字段顺序重排为：基础录入、客户信息、日期、财务、跟单、返利、快照、结算控制、系统辅助。
  - 已对现有 11 张订单管理区同步更新字段顺序和宽度。

## 最近验证结果

- `node --check public/app.js` 通过。
- `node --check server.js` 通过。
- `npm run check` 通过。
- `npm run test:frontend` 通过。
- `npm run test:p0` 通过。
- `npm run test:resource` 通过。
- `npm run test:product` 通过。
- `npm run test:team-scale` 通过。
- `npm run test:business-lock` 通过。
- `npm run test:order` 通过。
- `npm run test:jobs` 通过。
- `npm run test:diagnostics` 通过。
- 冒烟测试通过。
- 浏览器验证通过：
  - 粘贴 2 行 TSV 后新增 2 行并写入 4 个单元格。
  - 普通关联只保留 1 条。
  - 多选关联模式可保留 2 条关联。
  - 非法 lookup 配置返回 400。
  - 键盘导航可按方向键、Tab、Enter 移动并自动聚焦目标输入控件。
  - 单元格选中后输入字符进入编辑态；编辑态方向键不跳格；`Esc` 退出后不残留编辑样式；`Enter` 保存并同列下移。
  - 浏览器中 4 个前端模块均成功加载。
  - 浏览器中「业务模板」入口可见，P0 新版本脚本加载成功。
  - 浏览器中「资源档案」入口、筛选框、字段显示菜单可见。
  - 浏览器中同表规则视图验证通过：待审批只显示待审记录，已通过可用只显示已通过记录，领导审批只显示审批相关字段。
  - 浏览器中「产品信息」入口、产品信息区、名称关联、货号 lookup、标题合成展示验证通过。
  - 浏览器中「成员」「邀请」「产品信息」入口和新版脚本加载验证通过。
  - 浏览器中「业务锁定」入口和新版脚本加载验证通过。
  - 浏览器中右键菜单验证通过：普通数据区域不弹菜单，表名栏右键弹系统菜单，字段表头右键弹字段菜单。
  - 浏览器中字段管理验证通过：点击字段名不会误隐藏，只有勾选框控制隐藏；单元格格式只作用于当前选中单元格，表头和其他单元格不受影响。
  - 浏览器中字段排序验证通过：字段管理内点击「下移」后，表格列顺序立即变化并保存到后端。

## 明天可继续

- 批量粘贴时支持自动匹配单选项或自动新增缺失选项。
- 优化 lookup 字段展示：支持多值分隔、颜色继承、空值占位。
- 继续扩展前端自动化测试，覆盖真实 DOM 点击、粘贴和弹窗流程。
- 在批量事务接口上继续挂接销售发货、采购入库、充值、退款、红冲等业务脚本。
